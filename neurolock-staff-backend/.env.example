# MySQL Database Configuration
MYSQL_HOST=127.0.0.1
MYSQL_PORT=3307
MYSQL_USER=app
MYSQL_PASSWORD=app123
MYSQL_DATABASE=neurolock

# Encryption Key (256-bit hex for AES-256)
# Generate with: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
KEK_HEX=0123456789abcdef0123456789abcdef0123456789abcdef0123456789abcdef

# JWT Configuration
JWT_SECRET=your-super-secret-jwt-key-change-in-production
JWT_EXPIRATION=24h

# Server Configuration
PORT=4311
NODE_ENV=development

# Password Hashing
HASH_SALT_ROUNDS=10

# Legacy PostgreSQL (not used with MySQL)
# DATABASE_URL=postgres://username:password@localhost:5432/neuro_app