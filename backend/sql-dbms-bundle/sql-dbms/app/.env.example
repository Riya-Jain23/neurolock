# Copy to .env and adjust as needed
DB_HOST=127.0.0.1
DB_PORT=3306
DB_USER=app
DB_PASSWORD=app123
DB_NAME=neurolock

# 32-byte KEK (hex). Generate your own for real use: e.g. python -c "import os, binascii; print(binascii.hexlify(os.urandom(32)).decode())"
KEK_HEX=00112233445566778899aabbccddeeff00112233445566778899aabbccddeeff
